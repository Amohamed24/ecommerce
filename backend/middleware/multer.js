import multer from "multer";

// Creates storage config for users to upload images, review etc.

// const storage =  multer.diskStorage({
//     filename:function(req, file, callback){
//         callback(null, file.originalname)
//     }
// })

